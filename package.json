{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w penpot-export",
    "build": "npm run build -w penpot-export",
    "demo": "npm run generate-css -w demo"
  }
}
